Read schema_analysis/nmdc_biosample_slots.json and schema_analysis/gold_biosample_schema.json. Compare these schemas semantically to identify: 1) NMDC slots with no GOLD equivalents, 2) GOLD fields with no NMDC equivalents, 3) potential semantic matches. Return JSON with nmdc_unique, gold_unique, and potential_matches arrays. In potential_matches, include similarity_score (0.0-1.0), semantic_relationship (exact_match/near_match/semantic_match), and description. Use arrays for nmdc_field/gold_field ONLY when one field truly maps to multiple fields (like nmdc lat_lon to gold latitude+longitude). Keep individual 1:1 matches as strings, not arrays. Avoid over-bundling - create separate entries for distinct field pairs. Focus on biological/environmental metadata semantics.