Create ENVO ontology mappings for OpenStreetMap natural features using the OLS MCP search.

I have extracted unique natural features from OpenStreetMap data that need to be mapped to proper ENVO (Environmental Ontology) terms. For each feature, find the best matching ENVO term using the OLS MCP search functionality.

The natural features to map are in: outputs/natural_features.json

Please:
1. Read the natural features from the file
2. For each feature, use the OLS MCP to search for appropriate ENVO terms
3. Select the best matching ENVO term for each feature
4. Return a JSON object mapping each OpenStreetMap feature to its ENVO term with the format:
   {
     "osm_feature": {
       "envo_id": "ENVO:xxxxxxx",
       "envo_label": "descriptive label",
       "match_confidence": "high|medium|low",
       "reasoning": "brief explanation of why this match was chosen"
     }
   }

Focus on environmental and habitat terms that would be relevant for biosample location context.